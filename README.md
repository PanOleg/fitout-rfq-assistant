# Fit-out RFQ Assistant

Turns a fit-out bill of quantities into trade packages and supplier RFQs.
Work in progress — see the commit history.

```bash
composer setup
composer check   # style → static analysis → boundaries → tests
```
